{
  // Set the Jupyter notebook file root to the workspace folder
  "jupyter.notebookFileRoot": "${workspaceFolder}",

  // Do the same for python files
  "python.pythonPath": "${workspaceFolder}/venv/bin/python"
}
